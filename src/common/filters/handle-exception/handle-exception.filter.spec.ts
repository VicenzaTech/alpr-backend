import { HandleExceptionFilter } from './handle-exception.filter';

describe('HandleExceptionFilter', () => {
  it('should be defined', () => {
    expect(new HandleExceptionFilter()).toBeDefined();
  });
});
